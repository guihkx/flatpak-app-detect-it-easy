app-id: io.github.horsicq.detect-it-easy
runtime: org.kde.Platform
runtime-version: '6.8'
sdk: org.kde.Sdk
command: die

finish-args:
  - --device=dri
  - --share=ipc
  - --share=network # For uploading files to VirusTotal.
  - --socket=wayland
  - --socket=fallback-x11

modules:
  - name: detect-it-easy
    buildsystem: qmake
    sources:
      - type: git
        url: https://github.com/horsicq/DIE-engine.git
        commit: 6899db3a8d367b5480530d6f07ea50c1c05fe3cc
